/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static export for simple Vercel deployment
  // Remove this if you add API routes or server-side features
  // output: 'export',
};

export default nextConfig;
